{
  "name": "realfarm-helper",
  "version": "1.0.0",
  "description": "레알팜 모바일 게임 핼퍼 앱",
  "private": true,
  "scripts": {
    "serve": "vue-cli-service serve",
    "build": "vue-cli-service build",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "vue": "^3.3.4"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "@vue/cli-service": "^5.0.8",
    "gh-pages": "^6.0.0"
  },
  "browserslist": [
    "> 1%",
    "last 2 versions",
    "not dead"
  ]
}
